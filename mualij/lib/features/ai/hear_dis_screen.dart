import 'package:flutter/material.dart';

class HeartDiseaseScreen extends StatelessWidget {
  const HeartDiseaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Heart Disease Detection'),
      ),
      body: const Center(
        child: Text('Heart Disease Detection Model Will Be Implemented Here'),
      ),
    );
  }
}
